package com.github.fnb.assessment.katlego;

public class candidateDetails {
    /*private final String name = "Katlego";
    private final String surname = "Mohlala";
    private final String contact = "0813077848";*/

    /*public static void main(String[] args) throws IOException {
       // JSONObject candidateDetails = new JSONObject();
        Map<String,String> candidateDetails = new LinkedHashMap<String, String>();
        String filePath = "src/test.json";
        String line;
        String key ="";
        String value="";
        BufferedReader reader = new BufferedReader(new FileReader(filePath));
        //HashMap<String, String> map = new LinkedHashMap<String, String>();
        ObjectMapper mapper = new ObjectMapper();

        while ((line = reader.readLine()) != null) {
            String[] tokens = line.split(":", 2);
            if (tokens.length >= 2) {
                key = tokens[0];
                value = tokens[1];
                candidateDetails.put(key, value);

            } else {
                //System.out.println("leave out curly braces: " + line);
            }


        }
        for (String keys : candidateDetails.keySet())
        {
            System.out.println(keys + ":" + candidateDetails.get(keys));
        }
*//*

        try (FileWriter file = new FileWriter("candidate.json")) {
            file.write(res);
            file.flush();

        } catch (IOException e) {
            e.printStackTrace();
        } *//*
    }
*/
}
